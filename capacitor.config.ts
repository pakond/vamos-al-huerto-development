import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.vamosalhuerto',
  appName: 'Vamos al Huerto',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
